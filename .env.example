# Luma calendar API key (server-only). Create at https://lu.ma → Settings → API
LUMA_API_KEY=

# Set to true to use mock leaderboard data without a Luma key
MOCK_DATA=

# City shown in the leaderboard subtitle (e.g. "Buenos Aires", "San Francisco").
# Leave blank to omit the city from the subtitle.
CITY=
